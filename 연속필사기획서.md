# 연속 필사 기능 기획서

> 작성일: 2026-04-23
> 참조: Android 원본 프로젝트(`/Users/gangbohun/AndroidStudioProjects/Fillsa`)

---

## 1. 기능 목적

- 사용자의 **지속적인 필사 동기 부여**를 위한 핵심 기능
- 매일 필사한 기록을 바탕으로 **연속 필사 일수를 계산하고 시각적**으로 보여줌
- 단순 기록 이상의 의미를 부여하여, **습관 형성 및 성취감 강화**

---

## 2. 용어 정의

| 용어 | 설명 |
|---|---|
| **연속 필사** | 사용자가 연속으로 '오늘의 문장'을 당일 완전히 필사한 일수 |
| **필사 완료** | '오늘의 문장'을 타이핑 필사 또는 손글씨 업로드 중 하나라도 완료한 상태 |
| **최고 연속 필사** | 연속 필사일의 최대값 (저장만, 현재 UI 표기 없음 — 추후 디벨롭 예정) |

---

## 3. 연속 필사 계산 로직

| 조건 | 동작 |
|---|---|
| 이전일 + 오늘 모두 필사 완료 | `current_streak += 1` |
| 오늘만 필사 완료 (첫 시작 또는 연속 끊김) | `current_streak = 1` |
| 어제 미완료, 오늘 완료 | `current_streak = 1` |
| 오늘 미완료 | `current_streak = 0` |
| `current_streak > max_streak` | `max_streak = current_streak` 갱신 |

- **자정(00:30)** 기준으로 어제 필사 여부 체크 → 미완료이면 `current_streak = 0`
- 필사 완료 판별: `korTyping == korQuote` 또는 `engTyping == engQuote`
- 회원(로그인): 서버 API `GET /api/v1/member-streaks` 기준
- 비회원(비로그인): 로컬 DB(`streak_info` 테이블) 기준

---

## 4. UI 표시 요소

### 4-1. 앱바 — 연속 필사 일수 (Home / Calendar / List 공통)

- 최상단 Fillsa 로고 옆에 "🔥 N일" 형태로 표시
- 오늘 필사하지 않은 경우에도 현재 연속 일수가 표시됨
- 연속 필사 일수 영역 클릭 시 → Calendar 화면(현재 월)으로 이동
- 오늘 필사 완료 여부(`isTodayWritten`)에 따라 아이콘/색상 구분

### 4-2. 캘린더 — 날짜별 연속 필사 이모지

- 날짜 셀에 연속 필사 이모지(🔥) 표기
- 기존 필사완료 / 좋아요 표기와 함께 표시

### 4-3. 필사 완료/연속 팝업 (타이핑/이미지 화면 저장 후)

| 상태 | 팝업 종류 |
|---|---|
| 연속 필사 (이전일 + 오늘 완료) | 연속 필사 팝업 |
| 필사 완료 (오늘 첫 완료) | 필사 완료 팝업 |
| 필사 미완료 | 필사 미완료 팝업 |

---

## 5. 데이터 소스

| 상황 | 출처 |
|---|---|
| 회원 현재 streak | 서버 `GET /api/v1/member-streaks` → `MemberStreakResponse.currentStreak` |
| 비회원 현재 streak | 로컬 Drift DB `streak_info` 테이블 → `streakDateCount` |
| 캘린더 월별 streak 통계 | 서버 `monthlySummary.streakCount` (이미 구현됨) |

---

## 6. 현황 분석 (구현 시작 전 기준)

| 항목 | 상태 |
|---|---|
| `HomeAppBar` streak UI | 있음 — 하드코딩(`streakDays: 0`) |
| 캘린더 월별 streak 표시 (`CalendarCountSection`) | ✅ 완료 |
| 서버 API `GET /api/v1/member-streaks` | Flutter에 엔드포인트 없음 |
| 로컬 streak DB (`streak_info` Drift 테이블) | 없음 |
| 필사 완료 시 streak 업데이트 (`TypingViewModel`) | 없음 |
| 자정 streak 초기화 Worker | 없음 |
| `HomeState`에 streak 필드 | 없음 |

---

## 7. 개발 계획 (Phase별)

---

### ✅ Phase 1 — Domain 모델 & Repository 인터페이스

> 목표: 비즈니스 규칙과 계약을 정의한다. Data/Presentation에 의존하지 않는다.

#### 1-1. `StreakInfo` 도메인 모델

- **파일**: `lib/domain/model/streak_info.dart`
- **필드**:
  ```dart
  class StreakInfo {
    final int currentStreak;
    final bool isTodayWritten;
    final int maxStreak;
  }
  ```
- freezed 사용 (다른 모델과 동일한 방식)

#### 1-2. `StreakRepository` 인터페이스

- **파일**: `lib/domain/repository/streak_repository.dart`
- **메서드**:
  ```dart
  abstract class StreakRepository {
    Future<StreakInfo> getStreakInfo();
    Future<void> updateTodayStreak();      // 필사 완료 시 호출
    Future<void> checkYesterdayStreak();   // 자정 초기화 시 호출
  }
  ```

**완료 기준**:
- [x] `StreakInfo` freezed 모델 생성 및 빌드 완료
- [x] `StreakRepository` 인터페이스 정의

---

### ✅ Phase 2 — Data 계층

> 목표: 로컬 DB 테이블, 서버 API 엔드포인트, Repository 구현체를 만든다.

#### 2-1. 로컬 DB — Drift 테이블 추가

- **파일**: `lib/data/local/streak_info_entity.dart`
- **스키마**:
  ```dart
  class StreakInfoTable extends Table {
    TextColumn get date => text()();          // PK, 'yyyy-MM-dd'
    IntColumn get streakDateCount => integer()();
    BoolColumn get isDailyWritingCompleted => boolean()();
    @override
    Set<Column> get primaryKey => {date};
  }
  ```
- `lib/data/local/database.dart`에 테이블 등록

#### 2-2. 서버 API 엔드포인트 추가

- **파일**: `lib/data/network/fillsa_api.dart`
  ```dart
  @GET('/api/v1/member-streaks')
  Future<BaseResponse<MemberStreakResponse>> getMemberStreaks();
  ```
- **응답 모델**: `lib/domain/model/response/member_streak_response.dart`
  ```dart
  @freezed
  class MemberStreakResponse {
    final int currentStreak;
    final bool isTodayWritten;
  }
  ```

#### 2-3. `StreakRepositoryImpl` 구현

- **파일**: `lib/data/repository/streak_repository_impl.dart`
- **로직**:
  - 회원: 서버 API `getMemberStreaks()` 호출 → `StreakInfo` 변환
  - 비회원: 로컬 Drift DB에서 오늘 `StreakInfoEntity` 조회 → `StreakInfo` 변환
  - `updateTodayStreak()`:
    - 어제 `isDailyWritingCompleted == true` → `streakDateCount = 어제값 + 1`
    - 아니면 → `streakDateCount = 1`
    - `isDailyWritingCompleted = true`로 저장
    - `maxStreak` 갱신 (currentStreak > maxStreak이면 업데이트)
  - `checkYesterdayStreak()`:
    - 어제 엔트리의 `isDailyWritingCompleted == false`이면 → 오늘 `streakDateCount = 0` 삽입

**완료 기준**:
- [x] Drift 테이블 추가 및 `build_runner` 빌드 완료
- [x] `MemberStreakResponse` 모델 생성 및 빌드 완료
- [x] `fillsa_api.dart`에 엔드포인트 추가
- [x] `StreakRepositoryImpl` 구현 완료
- [x] DI(`injectable`) 등록 완료

---

### ✅ Phase 3 — Domain UseCase

> 목표: 단일 책임 UseCase 3개를 추가한다. `base_usecase.dart` 규약 준수.

#### 3-1. `GetStreakInfoUseCase`

- **파일**: `lib/domain/usecase/get_streak_info_usecase.dart`
- `streak_repository.getStreakInfo()` 호출

#### 3-2. `UpdateTodayStreakUseCase`

- **파일**: `lib/domain/usecase/update_today_streak_usecase.dart`
- `streak_repository.updateTodayStreak()` 호출

#### 3-3. `CheckYesterdayStreakUseCase`

- **파일**: `lib/domain/usecase/check_yesterday_streak_usecase.dart`
- `streak_repository.checkYesterdayStreak()` 호출

**완료 기준**:
- [x] UseCase 3개 생성
- [x] DI 등록 완료

---

### ✅ Phase 4 — Presentation

> 목표: HomeState에 streak를 연결하고, UI가 실제 데이터를 표시하게 한다.

#### 4-1. `HomeState`에 streak 필드 추가

- **파일**: `lib/presentation/state/HomeState.dart`
  ```dart
  StreakInfo? streakInfo
  ```

#### 4-2. `HomeViewModel`에 streak 로드 추가

- **파일**: `lib/presentation/viewmodels/home_viewmodel.dart`
- 앱 시작 / 화면 진입 시 `GetStreakInfoUseCase` 호출 → `state.streakInfo` 갱신
- 필사 완료 후 streak 갱신 (`UpdateTodayStreakUseCase` 호출 후 재조회)

#### 4-3. `HomeAppBar` 하드코딩 해제

- **파일**: `lib/presentation/ui/home/home_screen.dart`
- `streakDays: 0` → `state.streakInfo?.currentStreak ?? 0`
- `isTodayWritten` 여부에 따라 아이콘/색상 분기
- 클릭 시 Calendar 화면으로 이동 (go_router 사용)

#### 4-4. `TypingViewModel.save()` 필사 완료 판별 및 팝업 분기

- **파일**: `lib/presentation/viewmodels/typing_viewmodel.dart`
- 저장 성공 후:
  1. 필사 완료 여부 판별 (`korTyping == korQuote` 또는 `engTyping == engQuote`)
  2. 완료 시 `UpdateTodayStreakUseCase` 호출
  3. `GetStreakInfoUseCase`로 최신 streak 조회
  4. 팝업 분기:
     - `currentStreak > 1` → 연속 필사 팝업
     - `currentStreak == 1` → 필사 완료 팝업
     - 미완료 → 필사 미완료 팝업

#### 4-5. 캘린더 날짜 셀 streak 이모지 표시

- **파일**: `lib/presentation/ui/calendar/calendar_screen.dart`
- 날짜 셀에 해당 날짜의 `isDailyWritingCompleted` 기반으로 🔥 이모지 표시
- 기존 필사완료 / 좋아요 표기와 함께 표시

**완료 기준**:
- [x] `HomeState.streakInfo` 필드 추가 및 빌드 완료
- [x] `HomeViewModel` streak 로드 로직 추가
- [x] `HomeAppBar` 실제 데이터 연결 및 클릭 이동 구현
- [x] `TypingViewModel` 완료 판별 + UseCase 호출 + 팝업 분기 구현
- [x] 캘린더 날짜 셀 streak 이모지 표시

---

### ✅ Phase 5 — 자정 초기화 Worker

> 목표: 매일 자정 이후 어제 필사 여부를 체크해서 streak를 자동 초기화한다.

#### 5-1. `flutter_workmanager` 패키지 추가

- `pubspec.yaml`에 `workmanager` 추가

#### 5-2. `StreakWorker` 등록

- 매일 00:30 `CheckYesterdayStreakUseCase` 실행
- 앱 최초 실행 시 WorkManager에 등록
- Android / iOS 각각 등록 처리

**완료 기준**:
- [x] `workmanager` 패키지 추가
- [x] Worker 등록 및 `CheckYesterdayStreakUseCase` 연결 (앱 시작 시 `HomeViewModel`에서도 중복 체크)
- [x] `AndroidManifest.xml` 네이티브 설정 추가
- [ ] 자정 이후 실기기 체크 동작 확인 (수동 테스트 필요)

---

## 8. 진행 현황

| Phase | 내용 | 상태 |
|---|---|---|
| Phase 1 | Domain 모델 & Repository 인터페이스 | ✅ 완료 |
| Phase 2 | Data 계층 (DB + API + Repository 구현) | ✅ 완료 |
| Phase 3 | Domain UseCase | ✅ 완료 |
| Phase 4 | Presentation (State / VM / UI) | ✅ 완료 |
| Phase 5 | 자정 초기화 Worker | ✅ 완료 |

---

## 9. 디자인 구현 현황 (Android 원본 참조)

| UI 요소 | Android 원본 | Flutter 구현 결과 |
|---|---|---|
| AppBar streak > 0 | `icn_today_complete`(20dp) + "N일" (subtitle1, onBackground1) | ✅ SVG 아이콘 + 동일 텍스트 스타일 |
| AppBar streak == 0 | `icn_empty_daily_count`(24dp, 경고삼각형) | ✅ SVG 아이콘 |
| AppBar 경고 클릭 | BubbleTooltip ("연속 필사를 완료해 주세요!") | ✅ 말풍선 툴팁 (다크/라이트 색상 분기) |
| 연속 필사 팝업 | `icn_today_complete`(54dp) + "연속 필사 완료!" + 확인 버튼 | ✅ `DialogWithImage` |
| 필사 완료 팝업 | `icn_thumb_up`(54dp, #5C65FF) + "필사 완료!" + 확인 버튼 | ✅ `DialogWithImage` |
| 필사 미완료 팝업 | `icn_today_not_complete`/`_night`(54dp) + "필사가 완료되지 않았어요 :(" + "필사하기"/닫기 | ✅ `DialogWithImage` (다크/라이트 분기) |
| 캘린더 날짜 셀 | 1행: 노트+하트 아이콘, 2행: `icn_today_complete` (alpha 표시) | ✅ SVG 아이콘 + Opacity |

### 에셋 추가 목록
- `assets/images/icn_today_complete.svg` — 불꽃 아이콘 (fill #FF0000, stroke #FFEFCC)
- `assets/images/icn_empty_daily_count.svg` — 경고 삼각형 (fill #D3D5FF, stroke #5C65FF)
- `assets/images/icn_thumb_up.svg` — 엄지 아이콘 (fill #5C65FF)
- `assets/images/icn_today_not_complete.svg` — 달력 슬픈 얼굴 라이트 (stroke #FFCB5C)
- `assets/images/icn_today_not_complete_night.svg` — 달력 슬픈 얼굴 다크 (stroke #D3D5FF)

---

## 10. 개발 순서 요약

```
Phase 1 (모델 + 인터페이스)
  ↓
Phase 2-1 + 2-2 병렬 (로컬 DB + 서버 API)
  ↓
Phase 2-3 (StreakRepositoryImpl)
  ↓
Phase 3 (UseCase 3개)
  ↓
Phase 4-1 → 4-2 → 4-3 → 4-4 → 4-5 (Presentation 순차)
  ↓
Phase 5 (Worker)
```
